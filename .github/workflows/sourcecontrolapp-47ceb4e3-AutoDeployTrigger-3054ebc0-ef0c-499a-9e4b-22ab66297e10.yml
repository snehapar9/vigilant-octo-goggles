name: Trigger auto deployment for sourcecontrolapp-47ceb4e3

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ master ]
    paths:
    - '**'
    - '.github/workflows/sourcecontrolapp-47ceb4e3-AutoDeployTrigger-3054ebc0-ef0c-499a-9e4b-22ab66297e10.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.SOURCECONTROLAPP47CEB4E3_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.SOURCECONTROLAPP47CEB4E3_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.SOURCECONTROLAPP47CEB4E3_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          registryUrl: snehapark4apssacr.azurecr.io
          registryUsername: ${{ secrets.SOURCECONTROLAPP47CEB4E3_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.SOURCECONTROLAPP47CEB4E3_REGISTRY_PASSWORD }}
          containerAppName: sourcecontrolapp-47ceb4e3
          resourceGroup: EnvTest
          imageToBuild: snehapark4apssacr.azurecr.io/sourcecontrolapp-47ceb4e3:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


